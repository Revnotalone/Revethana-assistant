# ══════════════════════════════════════════════
#   REVETHANA ASSISTANT - Requirements
#   Python 3.9+ required
# ══════════════════════════════════════════════

# ── AI & API ──────────────────────────────────
google-generativeai>=0.8.0       # Gemini 2.5 Flash API

# ── Voice Recognition ─────────────────────────
SpeechRecognition>=3.10.0        # Speech-to-text library
pyaudio>=0.2.14                  # Microphone access (lihat catatan instalasi)

# ── Text-to-Speech ────────────────────────────
edge-tts>=6.1.9                  # Microsoft Edge TTS (suara natural gratis)

# ── Audio Playback ────────────────────────────
pygame>=2.5.0                    # Playback file audio MP3/WAV

# ── Desktop Control ───────────────────────────
pyautogui>=0.9.54                # GUI automation & volume control
psutil>=5.9.8                    # System info (CPU, RAM, baterai, proses)

# ── Environment & Config ──────────────────────
python-dotenv>=1.0.0             # Load .env file

# ── Terminal UI ───────────────────────────────
colorama>=0.4.6                  # Warna terminal Windows

# ── Keyboard Hotkeys (opsional) ───────────────
# keyboard>=0.13.5               # Global hotkeys (perlu admin di Windows)

# ══════════════════════════════════════════════
# CATATAN INSTALASI PYAUDIO DI WINDOWS:
#
# Jika `pip install pyaudio` gagal, coba:
#
# Opsi 1 - Pipwin (otomatis):
#   pip install pipwin
#   pipwin install pyaudio
#
# Opsi 2 - Install dari wheel (paling reliable):
#   Download dari: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
#   Pilih file sesuai Python version, contoh: PyAudio-0.2.14-cp311-cp311-win_amd64.whl
#   pip install PyAudio-0.2.14-cp311-cp311-win_amd64.whl
#
# Opsi 3 - Via conda:
#   conda install pyaudio
# ══════════════════════════════════════════════
